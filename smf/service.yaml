apiVersion: v1
kind: Service
metadata: { name: open5gs-smf, namespace: open5gs }
spec:
  selector: { app: open5gs-smf }
  ports:
    - { name: sbi, port: 7777, targetPort: 7777 }
    - { name: pfcp, port: 8805, targetPort: 8805, protocol: UDP }
