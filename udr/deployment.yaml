apiVersion: apps/v1
kind: Deployment
metadata:
  name: open5gs-udr
  namespace: open5gs
spec:
  replicas: 1
  selector: { matchLabels: { app: open5gs-udr } }
  template:
    metadata: { labels: { app: open5gs-udr } }
    spec:
      initContainers:
        - name: wait-mongo
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z mongodb 27017; do sleep 2; done"]
        - name: wait-nrf
          image: busybox:1.36
          command: ["sh", "-c", "until nc -z open5gs-nrf 7777; do sleep 2; done"]
      containers:
        - name: udr
          image: open5gs:v2.8.0
          imagePullPolicy: Never
          command: ["/opt/open5gs/bin/open5gs-udrd"]
          args: ["-c", "/etc/open5gs/udr.yaml"]
          ports: [{ containerPort: 7777 }]
          volumeMounts: [{ name: config, mountPath: /etc/open5gs }]
      volumes:
        - name: config
          configMap: { name: udr-config }
