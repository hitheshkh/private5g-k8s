apiVersion: v1
kind: Service
metadata: { name: open5gs-upf, namespace: open5gs }
spec:
  selector: { app: open5gs-upf }
  ports:
    - { name: pfcp, port: 8805, targetPort: 8805, protocol: UDP }
    - { name: gtpu, port: 2152, targetPort: 2152, protocol: UDP }
